flow

userbuy----->if quantity is less that the user required quantity -> usernotify_table---------
                                                                                            |
                                                                                            |
                                                                                            notifications table ---->application code to sent notification to users and manager 
                                                                                            |                                                                                           
                                                                                            |    
if stock less that 10----------->wanted_stocks table ----------------------------------------


the notification will be send on there respective profile based on the userid present in the notification table

notifications will be also sent while restocking there will be separate table for restock and joining these restock table
with the wanted_stocks table, usernotify_table the user can get to know the notification that they try to buy previouly




notification to the manager will be done by frequently cheacking the main stock table, the less quantity stock will be added to the notification table
then the application code poll to get that notification







I have create these databases
\use genworx;
CREATE TABLE users (
    user_id INT PRIMARY KEY AUTO_INCREMENT,
    full_name VARCHAR(100) NOT NULL,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    role ENUM('manager', 'user') NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

INSERT INTO users (full_name, username, password, role) VALUES
('John', 'manager1', 'manager123', 'manager'),
('Alice', 'alice', 'alice123', 'user'),
('Bob', 'bob', 'bob123', 'user'),
('Charlie', 'charlie', 'charlie123', 'user'),
('David', 'david', 'david123', 'user');


CREATE TABLE new_stocks (
    stock_id INT PRIMARY KEY,
    quantity INT NOT NULL,

    FOREIGN KEY (stock_id)
    REFERENCES stocks(stock_id)
    ON DELETE CASCADE
);

CREATE TABLE retail (
    retail_id INT PRIMARY KEY AUTO_INCREMENT,
    user_id INT NOT NULL,
    stock_id INT NOT NULL,
    quantity INT NOT NULL,
    purchased_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY(user_id)
    REFERENCES users(user_id),

    FOREIGN KEY(stock_id)
    REFERENCES stocks(stock_id)
);

CREATE TABLE less_count_stocks (

    alert_id INT PRIMARY KEY AUTO_INCREMENT,

    stock_id INT NOT NULL UNIQUE,

    current_quantity INT NOT NULL,

    notified BOOLEAN DEFAULT FALSE,

    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY(stock_id)
    REFERENCES stocks(stock_id)

);

CREATE TABLE notifications (

    notification_id INT PRIMARY KEY AUTO_INCREMENT,

    user_id INT NOT NULL,

    title VARCHAR(100) NOT NULL,

    message VARCHAR(255) NOT NULL,

    type ENUM(
        'LOW_STOCK',
        'NEW_STOCK',
        'PURCHASE',
        'GENERAL'
    ),

    is_read BOOLEAN DEFAULT FALSE,

    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY(user_id)
    REFERENCES users(user_id)

);

CREATE TABLE wanted_stocks(

    request_id INT PRIMARY KEY AUTO_INCREMENT,

    user_id INT NOT NULL,

    stock_id INT NOT NULL,

    requested_quantity INT NOT NULL,

    notified BOOLEAN DEFAULT FALSE,

    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY(user_id)
    REFERENCES users(user_id),

    FOREIGN KEY(stock_id)
    REFERENCES stocks(stock_id)

);


use genworx;

CREATE TABLE categories (
    category_id INT PRIMARY KEY AUTO_INCREMENT,
    category_name VARCHAR(100) NOT NULL UNIQUE
);

CREATE TABLE stocks (
    stock_id INT PRIMARY KEY AUTO_INCREMENT,
    stock_name VARCHAR(100) NOT NULL,
    quantity INT NOT NULL DEFAULT 0,
    category_id INT NOT NULL,

    FOREIGN KEY (category_id)
        REFERENCES categories(category_id)
        ON UPDATE CASCADE
        ON DELETE RESTRICT
);

INSERT INTO categories (category_name)
VALUES
('Electronics'),
('Groceries'),
('Furniture'),
('Clothing'),
('Books');


INSERT INTO stocks (stock_name, quantity, category_id) VALUES

-- Electronics (Category ID = 1)
('Laptop', 25, 1),
('Desktop Computer', 15, 1),
('Mobile Phone', 50, 1),
('Tablet', 20, 1),
('Smart Watch', 35, 1),
('Bluetooth Speaker', 40, 1),
('Wireless Earbuds', 60, 1),
('Gaming Keyboard', 30, 1),
('Gaming Mouse', 45, 1),
('Monitor', 18, 1),

-- Groceries (Category ID = 2)
('Rice', 120, 2),
('Wheat Flour', 90, 2),
('Sugar', 80, 2),
('Salt', 150, 2),
('Cooking Oil', 70, 2),
('Milk', 100, 2),
('Eggs', 200, 2),
('Tea Powder', 60, 2),
('Coffee Powder', 55, 2),
('Biscuits', 140, 2),

-- Furniture (Category ID = 3)
('Dining Table', 12, 3),
('Office Chair', 25, 3),
('Study Table', 18, 3),
('Wardrobe', 10, 3),
('Bookshelf', 15, 3),
('Sofa', 8, 3),
('Coffee Table', 14, 3),
('Bed Frame', 11, 3),
('TV Stand', 17, 3),
('Shoe Rack', 22, 3),

-- Clothing (Category ID = 4)
('T-Shirt', 100, 4),
('Jeans', 70, 4),
('Shirt', 65, 4),
('Hoodie', 40, 4),
('Jacket', 30, 4),
('Shorts', 50, 4),
('Sweater', 35, 4),
('Socks', 200, 4),
('Cap', 80, 4),
('Track Pants', 55, 4),

-- Books (Category ID = 5)
('Data Structures in C++', 20, 5),
('Operating System Concepts', 18, 5),
('Computer Networks', 22, 5),
('Database Management Systems', 16, 5),
('Clean Code', 14, 5),
('Design Patterns', 12, 5),
('The Pragmatic Programmer', 10, 5),
('Introduction to Algorithms', 8, 5),
('Artificial Intelligence', 15, 5),
('Competitive Programming Handbook', 25, 5);



=====> these are table and values i have create

